## Agent skills

### Issue tracker

Issue 与 spec 以 GitHub issues 跟踪（使用 `gh` CLI）。见 `docs/agents/issue-tracker.md`。

### Triage labels

五类 triage 标签使用默认词表：`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`。见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文：根目录 `CONTEXT.md` + `docs/adr/`。见 `docs/agents/domain.md`。
